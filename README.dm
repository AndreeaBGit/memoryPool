In order to run the tests open testBuild.sh and set GTEST_INCLUDE and GTEST_LIB to point to the corresponding google test include and libraries on your system.
The run testBuild.sh and after memPoolTest executable to run the tests.
